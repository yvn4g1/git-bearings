import { escapeHtml, renderOverviewHtml } from "./overviewHtmlRenderer";
import { createOverviewPresentation } from "./overviewPresentation";
import type { GitMapItem, GitMapPresentation } from "./gitMapPresentation";
import type { CommitGraphPresentation } from "./commitGraphPresentation";
import { formatGoalCommand, goalCatalog, rankedGoals, resolveGoal, type GoalTarget } from "../domain/goal";

export function renderGitMapHtml(presentation: GitMapPresentation, nonce: string): string {
  let content = presentation.status === "available" ? availableMap(presentation) : `<p class="message"${presentation.unavailableReason ? ` title="${escapeHtml(presentation.unavailableReason)}"` : ""}>${escapeHtml(presentation.message ?? "")}</p>`;
  return `<!DOCTYPE html><html lang="ja"><head><meta charset="UTF-8" /><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'nonce-${nonce}'; script-src 'nonce-${nonce}';" /><meta name="viewport" content="width=device-width, initial-scale=1.0" /><title>Git Bearings: Git Map</title><style nonce="${nonce}">
.local-ref { fill:var(--vscode-editor-background); stroke:var(--vscode-foreground); stroke-width:1.5; } .ref-line,.head-line { fill:none; stroke:var(--vscode-foreground); stroke-width:1.5; } .head-label { fill:var(--vscode-foreground); font-size:11px; font-weight:700; } .remote-tracking-box,.remote-tracking-line { fill:none; stroke:var(--vscode-descriptionForeground); stroke-width:1.5; stroke-dasharray:4 3; } .remote-tracking-hidden { display:none; } .unborn-head { font-weight:700; } .unborn-branch { border:1px solid var(--vscode-foreground); border-radius:4px; padding:2px 6px; } .fact-state { border-left:2px solid var(--vscode-foreground); padding-left:8px; } .unknown-state { border:1px dashed var(--vscode-descriptionForeground); padding:5px 8px; } .unknown-symbol { font-weight:700; margin-right:6px; } .warning-state { border:1px solid var(--vscode-notificationsWarningIcon-foreground); border-radius:3px; padding:5px 8px; } .warning-symbol { font-weight:700; margin-right:6px; } .selected-state { outline:2px solid var(--vscode-focusBorder); outline-offset:3px; } .related-state { outline:1px dashed var(--vscode-focusBorder); outline-offset:2px; } .prediction-node { fill:none; stroke:var(--vscode-descriptionForeground); stroke-width:2; stroke-dasharray:4 3; } .prediction-edge { fill:none; stroke:var(--vscode-descriptionForeground); stroke-width:1.5; stroke-dasharray:4 3; } .prediction-label { fill:var(--vscode-descriptionForeground); font-size:10px; font-weight:700; }
:root { color:var(--vscode-foreground); font-family:var(--vscode-editor-font-family,var(--vscode-font-family)); } body { margin:0; padding:8px 10px; line-height:1.25; overflow-x:hidden; } h1 { font-size:1em; margin:0; } h2 { margin:0 0 3px; font-size:.68em; letter-spacing:.06em; color:var(--vscode-descriptionForeground); } h3 { font-size:.86em; } p { margin:1px 0; } button,input { font:inherit; } .repository,.muted,.message { color:var(--vscode-descriptionForeground); overflow-wrap:anywhere; } .operation { margin:5px 0; font-weight:700; } .preview-banner { margin:5px 0; border:2px dashed var(--vscode-focusBorder); padding:4px 6px; font-weight:700; } .preview-overlay { border-left:2px dashed var(--vscode-descriptionForeground); margin:4px 0; padding:2px 4px; } .surface { margin-top:5px; max-width:none; min-width:0; } .map-scroll { min-width:0; overflow-x:hidden; padding:2px 0 5px; } .mental-map { width:100%; min-width:0; } .map-zone { min-width:0; padding:3px 4px; } .map-history { min-width:0; padding:3px 0 8px; border-bottom:1px solid var(--vscode-panel-border); } .map-context { display:grid; grid-template-columns:minmax(170px,220px) 82px minmax(145px,190px) minmax(320px,1fr); align-items:start; gap:8px; padding-top:4px; } .map-context .map-zone { overflow-wrap:anywhere; } .map-working { max-width:220px; } .map-staging { max-width:190px; } .map-flow { color:var(--vscode-descriptionForeground); font-weight:700; text-align:center; white-space:nowrap; } .map-flow span { display:inline; margin-left:4px; font-size:.66em; font-weight:400; white-space:nowrap; } .map-context-flow { padding-top:11px; } .context-commit { margin:0 0 0 6px; font-size:.72em; color:var(--vscode-descriptionForeground); } .map-staging .compact-fact,.map-staging .context-commit { display:inline; } .map-remote { border-left:1px solid var(--vscode-panel-border); padding-left:8px; } .map-remote h2,.map-remote > .context-commit { display:inline; } .map-remote h2 { margin-right:6px; } .map-remote > .remote { margin-top:2px; padding-left:0; border-left:0; } .map-remote > .remote > strong { margin-right:8px; } .map-remote > .remote > dl { display:inline-flex; flex-wrap:wrap; gap:2px 10px; vertical-align:baseline; } .map-remote > .remote > dl div { display:inline-flex; gap:3px; margin:0; } .map-remote .unknown-state { display:inline-block; width:auto; max-width:100%; margin:2px 0; padding:2px 5px; } .map-remote > .muted { display:block; margin-top:2px; font-size:.78em; } dl { margin:0; font-size:.92em; } dl div { display:grid; grid-template-columns:minmax(62px,auto) minmax(0,1fr); gap:4px; margin:1px 0; } dt { color:var(--vscode-descriptionForeground); } dd { margin:0; overflow-wrap:anywhere; } .compact-fact { font-size:.92em; } .staging { border-left:2px solid var(--vscode-panel-border); } .stash-shelf { float:none; margin:4px 0 0; border:1px solid var(--vscode-panel-border); border-radius:2px; padding:2px 4px; } .graph-scroll { min-width:0; width:100%; max-width:100%; overflow-x:auto; overflow-y:hidden; padding:0 0 2px; } .commit-graph { display:block; } .graph-edge { fill:none; stroke:var(--vscode-panel-border); stroke-width:2; } .graph-node { fill:var(--vscode-editor-background); stroke:var(--vscode-foreground); stroke-width:2; } .graph-node-current { stroke:var(--vscode-focusBorder); stroke-width:3; } .graph-node-base { fill:var(--vscode-textLink-foreground); } .graph-node-selected { stroke-width:4; stroke-dasharray:none; } .graph-node-related { stroke-width:3; stroke-dasharray:3 2; } .graph-text { fill:var(--vscode-foreground); font-size:12px; } .graph-subject { fill:var(--vscode-descriptionForeground); } .graph-role { fill:var(--vscode-descriptionForeground); font-size:8px; font-weight:400; } .current-location-label { fill:var(--vscode-foreground); font-size:11px; font-weight:700; } .graph-omission { fill:var(--vscode-descriptionForeground); font-size:11px; } .remote { margin-top:3px; padding-left:4px; border-left:2px solid var(--vscode-panel-border); } .detail-pane { border-top:1px solid var(--vscode-panel-border); margin-top:4px; padding-top:3px; max-width:960px; } .detail-nav { display:flex; gap:10px; margin:0; } .detail-tab { color:var(--vscode-descriptionForeground); background:transparent; border:0; border-bottom:1px solid transparent; padding:2px 0; cursor:pointer; } .detail-tab:hover,.detail-tab:focus-visible { color:var(--vscode-foreground); border-bottom-color:var(--vscode-focusBorder); outline:none; } .detail-tab-active { color:var(--vscode-foreground); border-bottom:2px solid var(--vscode-focusBorder); } details { margin-top:5px; } summary { cursor:pointer; } .overview { margin-top:5px; } .overview-section { border-top:1px solid var(--vscode-panel-border); padding:5px 0; } .overview-section h3 { margin:0 0 3px; font-size:.9em; } .overview-repository { display:none; } .overview-message,.overview-meaning { color:var(--vscode-descriptionForeground); } .overview-operation { border-left:3px solid var(--vscode-notificationsWarningIcon-foreground); padding-left:6px; font-weight:700; } @media (max-width:760px) { .map-context { grid-template-columns:minmax(0,1fr) 70px minmax(0,1fr); } .map-remote { grid-column:1 / -1; border-left:0; border-top:1px solid var(--vscode-panel-border); padding:4px 4px 0; } } @media (prefers-reduced-motion: reduce) { *,*::before,*::after { animation:none !important; transition:none !important; } }
</style></head><body><h1>Git Bearings: Git Map</h1>${presentation.repository ? `<p class="repository">${escapeHtml(presentation.repository)}</p>` : ""}${presentation.operationBanner ? `<p class="operation warning-state"><span class="warning-symbol">!</span>${escapeHtml(presentation.operationBanner)}</p>` : ""}${presentation.commandPreview?.banner ? `<aside class="preview-banner">PREVIEW<br /><span>Repositoryは変更されていません</span>${presentation.commandPreview.stale ? `<p>このPreviewは変更前の状態を元にしています</p><button data-preview-action="recalculate">新しい状態で再計算</button><button data-preview-action="clear">現在地へ戻る</button>` : ""}</aside>` : ""}${content}<script nonce="${nonce}">const v=acquireVsCodeApi(),save=()=>{const g=document.querySelector('.graph-scroll');v.setState({scrollY:window.scrollY,scrollLeft:g?g.scrollLeft:0,draft:document.querySelector('[data-command-input]')?.value||''});},restore=()=>{const s=v.getState();if(!s)return;window.scrollTo(0,s.scrollY||0);const g=document.querySelector('.graph-scroll');if(g)g.scrollLeft=s.scrollLeft||0;const i=document.querySelector('[data-command-input]');if(i&&s.draft)i.value=s.draft;const p=document.querySelector('[data-primary-selection="true"]');if(p){const r=p.getBoundingClientRect(),w=window.innerWidth,h=window.innerHeight;if(r.left<0||r.right>w||r.top<0||r.bottom>h)p.scrollIntoView({block:'nearest',inline:'nearest'});}};restore();window.addEventListener('scroll',save,{passive:true});document.querySelector('.graph-scroll')?.addEventListener('scroll',save,{passive:true});document.addEventListener('click',e=>{const g=e.target.closest('[data-goal-preview]');if(g){try{v.postMessage(JSON.parse(g.dataset.goalPreview||''));}catch{}return;}const b=e.target.closest('[data-goal-branch-preview]');if(b){const i=b.closest('.overview-section')?.querySelector('[data-goal-branch]');v.postMessage({type:'goalPreview',goalId:'createBranch',stepId:'create',target:{kind:'newBranch',branchName:i?.value||''}});return;}const j=e.target.closest('[data-goal-jump]');if(j){document.getElementById(j.dataset.goalJump||'')?.scrollIntoView({block:'nearest'});return;}const a=e.target.closest('[data-preview-action]');if(a){v.postMessage({type:a.dataset.previewAction});return;}const h=e.target.closest('[data-history-index]');if(h){v.postMessage({type:'selectHistory',index:Number(h.dataset.historyIndex)});return;}const x=e.target.closest('[data-example]');if(x){const i=document.querySelector('[data-command-input]');if(i)i.value=x.dataset.example||'';save();return;}const t=e.target.closest('[data-selection]');if(!t)return;let selection;try{selection=JSON.parse(t.dataset.selection||'');}catch{return;}save();v.postMessage({type:'select',selection});});document.addEventListener('submit',e=>{const f=e.target.closest('[data-command-form]');if(!f)return;e.preventDefault();const i=f.querySelector('[data-command-input]');v.postMessage({type:'analyze',input:i?.value||''});});document.addEventListener('keydown',e=>{const t=e.target.closest('[data-selection]');if((e.key==='Enter'||e.key===' ')&&t){e.preventDefault();t.click();}});</script></body></html>`;
}

function availableMap(presentation: GitMapPresentation): string {
  const remote = presentation.remoteMessage ? (presentation.remoteUnavailableReason ? unknown({ label: presentation.remoteMessage, message: "" }, presentation.remoteUnavailableReason) : `<p class="muted">${escapeHtml(presentation.remoteMessage)}</p>`) : presentation.remotes.map((item) => `<section class="remote fact-state${visualClass(item.visualState)}"${selectionAttr({ kind: "remote", remoteName: item.name }, item.visualState === "selected")}><strong>${escapeHtml(item.name)}</strong>${facts(item.facts)}${unknown(item.liveRemote)}</section>`).join("");
  const upstream = `<section class="remote${visualClass(presentation.upstreamVisualState)}"${presentation.upstreamUnavailableReason ? ` title="${escapeHtml(presentation.upstreamUnavailableReason)}"` : ""}${presentation.upstreamSelection ? selectionAttr({ kind: "upstream", ...presentation.upstreamSelection }, presentation.upstreamVisualState === "selected") : ""}>${facts(presentation.upstream)}</section>`;
  const inspect = presentation.explanation ? `<p>選択中: ${escapeHtml(presentation.explanation.identity)}</p><p class="detail-level1">${escapeHtml(presentation.explanation.level1)}</p>${commitDetail(presentation.commitDetail)}<details class="detail-more"><summary>もっと詳しく</summary><p>${escapeHtml(presentation.explanation.level2)}</p></details>` : `<p>選択中: ${escapeHtml(presentation.detailIdentity ?? "Overview")}</p>${renderOverviewHtml(createOverviewPresentation(presentation.detailSnapshot))}`;
  const detail = presentation.detailMode === "commandInput" ? commandDetail(presentation.commandPreview) : presentation.detailMode === "goal" ? goalDetail(presentation) : inspect;
  const preview = presentation.commandPreview?.map ?? undefined;
  const workingOverlay = regionOverlay("Working Tree → Staging", preview?.workingTree, preview?.warnings.workingTree, preview?.unknowns.workingTree);
  const stagingOverlay = regionOverlay("Staging ← Working Tree", preview?.staging, preview?.warnings.staging, preview?.unknowns.staging);
  const stashOverlay = regionOverlay("Stash Shelf Prediction", preview?.stash, preview?.warnings.stash, preview?.unknowns.stash);
  const localOverlay = regionOverlay("Local Prediction", preview?.local, preview?.warnings.local, preview?.unknowns.local);
  const remoteOverlay = regionOverlay("Remote / external wait", preview?.remote, preview?.warnings.remote, preview?.unknowns.remote);
  const detailOpen = presentation.detailMode === "commandInput" || presentation.detailMode === "goal" ? " open" : "";
  const tab = (mode: "inspect" | "commandInput" | "goal", action: string, label: string) => `<button class="detail-tab${presentation.detailMode === mode ? " detail-tab-active" : ""}" type="button" data-preview-action="${action}"${presentation.detailMode === mode ? ' aria-current="page"' : ""}>${label}</button>`;
  return `<main class="surface" aria-label="Git Map"><div class="map-scroll"><div class="mental-map"><section class="map-zone map-history"><h2>LOCAL COMMIT HISTORY</h2>${localOverlay}${renderCommitGraph(presentation.graph)}</section><div class="map-context" aria-label="Working Tree、Staging、Remoteの現在の文脈"><section class="map-zone map-working fact-state${visualClass(presentation.workingTree.visualState)}"${selectionAttr({ kind: "workingTree", section: "overview" }, presentation.workingTree.visualState === "selected")}><h2>WORKING TREE</h2>${workingTree(presentation.workingTree)}${workingOverlay}</section><p class="map-flow map-context-flow" aria-label="git addは内容をStagingへ記録">→<span>git add · 内容を記録</span></p><section class="map-zone map-staging staging fact-state${visualClass(presentation.staging.visualState)}"${selectionAttr({ kind: "staging" }, presentation.staging.visualState === "selected")}><h2>STAGING</h2><p class="compact-fact">Staged: <strong>${presentation.staging.stagedCount}</strong></p><p class="context-commit" aria-label="commitはStaging内容からcommitを作成">commit ↑ Local History</p>${stash(presentation.stash)}${stashOverlay}${stagingOverlay}</section><section class="map-zone map-remote"><h2>REMOTE / UPSTREAM CONTEXT</h2><p class="context-commit">current commit ↔ tracking context</p>${remote}${upstream}${remoteOverlay}<p class="muted">remote-trackingは最後に取得した情報です。live Remoteは未確認です。</p></section></div></div></div><section class="detail-pane"><nav class="detail-nav" aria-label="Detail表示">${tab("inspect", "detailInspect", "現在の選択")}${tab("commandInput", "detailCommand", "このコマンド何する？")}${tab("goal", "detailGoal", "こうしたい。何を使う？")}</nav><details class="detail-content"${detailOpen}><summary>Detail / Command Preview / Goal</summary>${detail}</details></section></main>`;
}

function goalDetail(presentation: GitMapPresentation): string { if (presentation.detailSnapshot.kind !== "available") return `<p class="muted">GoalはRepositoryのFactを取得できてから案内します。</p>`; const state = presentation.detailSnapshot.state; const names = { save: "変更を残したい", integrate: "他の変更を取り込みたい", workElsewhere: "別の場所で作業したい", undo: "やり直したい" }; const recommended = rankedGoals(state); const catalog = (["save", "integrate", "workElsewhere", "undo"] as const).map((category) => `<details><summary>${names[category]}</summary>${goalCatalog.filter((goal) => goal.category === category).map((goal) => goalCard(resolveGoal(state, { goalId: goal.id }), state)).join("")}</details>`).join(""); return `<section><h3>こうしたい</h3>${recommended.length ? `<p>今おすすめ: ${recommended.map((id) => `<button type="button" data-goal-jump="goal-${id}">${escapeHtml(goalCatalog.find((goal) => goal.id === id)!.label)}</button>`).join("")}</p>` : ""}<p class="muted">目的を選ぶと、その手順だけを確認できます。</p>${catalog}</section>`; }
function goalCard(resolution: ReturnType<typeof resolveGoal>, state: import("../domain/repositoryState").RepositoryState): string { const preview = (step: { readonly id: string }, target?: GoalTarget) => `<button type="button" data-goal-preview="${escapeHtml(JSON.stringify({ type: "goalPreview", goalId: resolution.goalId, stepId: step.id, ...(target ? { target } : {}) }))}">Preview</button>`; const recommendation = (item: NonNullable<typeof resolution.primary>, kind: "おすすめ" | "別の方法") => `<p>${kind}: ${escapeHtml(item.label)}${item.advanced ? "（advanced）" : ""}</p><ol>${item.steps.map((step, index) => `<li>Step ${index + 1}: <code>${escapeHtml(formatGoalCommand(step.command))}</code>${index === 0 ? ` ${preview(step)}` : ""}</li>`).join("")}</ol>`; const choices = (resolution.targetChoices ?? []).map((target) => { const next = resolveGoal(state, { goalId: resolution.goalId, target }); const step = next.primary?.steps[0]; const label = target.kind === "branch" || target.kind === "newBranch" ? target.branchName : target.kind === "remote" ? target.remote : `${target.remote}/${target.branch}`; return step ? `<p><button type="button" data-goal-preview="${escapeHtml(JSON.stringify({ type: "goalPreview", goalId: resolution.goalId, stepId: step.id, target }))}">${escapeHtml(label)} をPreview</button></p>` : ""; }).join(""); const input = resolution.goalId === "createBranch" && resolution.outcome === "needsInput" ? `<label>branch名 <input data-goal-branch maxlength="256" /></label> <button type="button" data-goal-branch-preview="true">Preview</button>` : ""; return `<section id="goal-${resolution.goalId}" class="overview-section"><strong>${escapeHtml(resolution.label)}</strong><p>${escapeHtml(resolution.current)}</p><p>${escapeHtml(resolution.change)}</p>${resolution.outcome === "noOperationNeeded" ? "<p>現在、この目的のために行う操作はありません。</p>" : ""}${resolution.primary ? recommendation(resolution.primary, "おすすめ") : ""}${resolution.alternative ? recommendation(resolution.alternative, "別の方法") : ""}${choices}${input}${resolution.notice ? `<p class="muted">注意: ${escapeHtml(resolution.notice)}</p>` : ""}</section>`; }

function commandDetail(preview: import("./commandPreview").CommandPreviewPresentation | undefined): string { const active = preview?.active; const sections = preview?.sections.map((section) => `<section class="overview-section"><h3>${escapeHtml(section.title)}</h3>${section.lines.map((line) => `<p>${escapeHtml(line)}</p>`).join("")}</section>`).join("") ?? ""; return `<form data-command-form><label>Git command <input data-command-input maxlength="4096" value="${escapeHtml(active?.rawInput ?? "")}" /></label><button type="submit">Analyze</button></form>${preview?.examples.length ? `<p>今の状態と関係する例: ${preview.examples.map((item) => `<button type="button" data-example="${escapeHtml(item)}">${escapeHtml(item)}</button>`).join("")}</p>` : ""}${sections}${preview?.history.length ? `<section class="overview-section"><h3>Session history</h3>${preview.history.map((item, index) => `<button type="button" data-history-index="${index}">${escapeHtml(item.label)}${item.stale ? " (stale)" : ""}</button>`).join("")}</section>` : ""}`; }
function regionOverlay(title: string, lines: readonly string[] | undefined, warnings: readonly string[] | undefined, unknowns: readonly string[] | undefined): string { if (!lines?.length && !warnings?.length && !unknowns?.length) return ""; return `<aside class="preview-overlay"><strong>${escapeHtml(title)}</strong>${(lines ?? []).map((line) => `<p>${escapeHtml(line)}</p>`).join("")}${(warnings ?? []).map((line) => `<p class="warning-state"><span class="warning-symbol">!</span>${escapeHtml(line)}</p>`).join("")}${(unknowns ?? []).map((line) => `<p class="unknown-state"><span class="unknown-symbol">?</span>${escapeHtml(line)}</p>`).join("")}</aside>`; }

function commitDetail(state: import("../domain/commitDetail").CommitDetailState | undefined): string {
  if (!state || state.kind === "idle") return "";
  if (state.kind === "loading") return `<section class="commit-detail" aria-label="Commit詳細"><h3>Commit詳細</h3><p>Commit詳細を読み込み中…</p></section>`;
  if (state.kind === "unavailable") return `<section class="commit-detail" aria-label="Commit詳細"><h3>Commit詳細</h3><p>Commit詳細を取得できません</p></section>`;
  const detail = state.detail;
  const paths = detail.changedFiles.slice(0, 100);
  const remaining = detail.changedFileCount - paths.length;
  return `<section class="commit-detail" aria-label="Commit詳細"><h3>Commit詳細</h3><dl><div><dt>Full hash</dt><dd>${escapeHtml(detail.fullHash)}</dd></div><div><dt>Author</dt><dd>${escapeHtml(detail.author)}</dd></div><div><dt>Authored at</dt><dd>${escapeHtml(detail.authoredAt)}</dd></div><div><dt>Parents</dt><dd>${detail.parents.length ? detail.parents.map(escapeHtml).join("<br />") : "なし"}</dd></div><div><dt>Changed files</dt><dd>${detail.changedFileCount}</dd></div></dl>${paths.length ? `<ul>${paths.map((path) => `<li>${escapeHtml(path)}</li>`).join("")}</ul>` : ""}${remaining > 0 ? `<p>他 ${remaining} 件</p>` : ""}</section>`;
}

function renderCommitGraph(graph: CommitGraphPresentation): string {
  if (graph.kind === "unborn") return `<p class="unborn-head${visualClass(graph.unbornHeadVisualState)}"${selectionAttr({ kind: "head" }, graph.unbornHeadVisualState === "selected")}>HEAD</p><p>↓</p><p><span class="unborn-branch${visualClass(graph.unbornBranchVisualState)}"${selectionAttr({ kind: "branch", branchName: graph.unbornBranch ?? "" }, graph.unbornBranchVisualState === "selected")}>[ ${escapeHtml(graph.unbornBranch ?? "")} ]</span></p><p class="muted">${escapeHtml(graph.message ?? "")}</p>`;
  if (graph.kind !== "graph") return `<p class="muted">${escapeHtml(graph.message ?? "")}</p>`;
  const edges = graph.edges.map((edge) => `<path class="graph-edge" d="M ${edge.parentX + 7} ${edge.parentY} C ${edge.parentX + 42} ${edge.parentY}, ${edge.childX - 42} ${edge.childY}, ${edge.childX - 7} ${edge.childY}" />`).join("");
  const omissions = graph.omissions.map((omission) => `<text class="graph-omission" x="${omission.x}" y="${omission.y + 4}" text-anchor="middle">… 履歴を省略 …</text>`).join("");
  const refs = graph.localBranches.map((ref) => `<g class="local-branch-ref${visualClass(ref.visualState)}"${selectionAttr({ kind: "branch", branchName: ref.label }, ref.visualState === "selected")}><path class="ref-line" d="M ${ref.connector.fromX} ${ref.connector.fromY} L ${ref.connector.toX} ${ref.connector.toY}" /><rect class="local-ref" x="${ref.bounds.left}" y="${ref.bounds.top}" width="${ref.bounds.width}" height="${ref.bounds.height}" rx="5" /><text class="graph-text" x="${ref.x}" y="${ref.y + 2}" text-anchor="middle">${escapeHtml(ref.label)}</text></g>`).join("");
  const tracking = graph.remoteTrackingRefs.map((ref) => `<g class="remote-tracking-ref${ref.revealed ? "" : " remote-tracking-hidden"}${visualClass(ref.visualState)}"><path class="remote-tracking-line" d="M ${ref.connector.fromX} ${ref.connector.fromY} L ${ref.connector.toX} ${ref.connector.toY}" /><rect class="remote-tracking-box" x="${ref.bounds.left}" y="${ref.bounds.top}" width="${ref.bounds.width}" height="${ref.bounds.height}" rx="2" /><text class="graph-text" x="${ref.x}" y="${ref.y + 2}" text-anchor="middle">remote-tracking: ${escapeHtml(ref.label)}</text></g>`).join("");
  const head = graph.head ? `<g class="head-pointer${visualClass(graph.head.visualState)}"${selectionAttr({ kind: "head" }, graph.head.visualState === "selected")}><path class="head-line" d="M ${graph.head.x} ${graph.head.y + 8} L ${graph.head.x} ${graph.head.targetY}" /><text class="head-label" x="${graph.head.x}" y="${graph.head.y}" text-anchor="middle">HEAD</text></g>` : "";
  const predictionEdges = (graph.predictionEdges ?? []).map((edge) => `<path class="prediction-edge" d="M ${edge.fromX + 7} ${edge.fromY} L ${edge.toX - 7} ${edge.toY}" />`).join("");
  const predictionPointers = (graph.predictionPointers ?? []).map((pointer) => `<g class="prediction-pointer"><path class="prediction-edge" d="M ${pointer.x} ${pointer.y + 6} L ${pointer.toX} ${pointer.toY}" />${pointer.kind === "branch" ? `<rect class="prediction-node" x="${pointer.x - 42}" y="${pointer.y - 14}" width="84" height="18" rx="4" />` : ""}<text class="prediction-label" x="${pointer.x}" y="${pointer.y - 2}" text-anchor="middle">${escapeHtml(pointer.kind === "head" ? "HEAD → predicted branch" : `${pointer.label} (predicted)`)}</text></g>`).join("");
  const predictions = (graph.predictionCommits ?? []).map((node) => `<g class="prediction-commit${node.visualState ? ` ${node.visualState}-state` : ""}"><circle class="prediction-node" cx="${node.x}" cy="${node.y}" r="7" /><text class="prediction-label" x="${node.x + 13}" y="${node.y - 4}">${escapeHtml(node.label)}</text><text class="prediction-label" x="${node.x + 13}" y="${node.y + 12}">◌ ${escapeHtml(node.description)}</text></g>`).join("");
  const nodes = graph.nodes.map((node) => {
    const roles = [
      ...(node.roles.includes("base") ? ["base"] : []),
      ...(node.roles.includes("mergeBase") ? ["merge-base (common ancestor)"] : []),
    ].join(" / ");
    const current = node.roles.includes("current") ? `<text class="current-location-label" x="${node.x + 13}" y="${node.y + (roles ? 43 : 28)}">↑ あなたは今ここ</text>` : "";
    return `<g class="fact-commit${visualClass(node.visualState)}"${selectionAttr({ kind: "commit", commitId: node.commitId }, node.visualState === "selected")}><circle class="graph-node${node.roles.includes("current") ? " graph-node-current" : ""}${node.roles.includes("base") ? " graph-node-base" : ""}${node.visualState ? ` graph-node-${node.visualState}` : ""}" cx="${node.x}" cy="${node.y}" r="7" /><text class="graph-text" x="${node.x + 13}" y="${node.y - 5}">${escapeHtml(node.shortId)}</text><text class="graph-text graph-subject" x="${node.x + 13}" y="${node.y + 12}">${escapeHtml(node.subject)}</text>${roles ? `<text class="graph-role" x="${node.x + 13}" y="${node.y + 27}">${roles}</text>` : ""}${current}</g>`;
  }).join("");
  return `<div class="graph-scroll"><svg class="commit-graph" role="img" aria-label="Commit Graph: old to new" width="${graph.width}" height="${graph.height}" viewBox="0 0 ${graph.width} ${graph.height}" xmlns="http://www.w3.org/2000/svg">${edges}${omissions}${refs}${tracking}${head}${nodes}${predictionEdges}${predictionPointers}${predictions}</svg></div>`;
}

function workingTree(tree: import("./gitMapPresentation").WorkingTreePresentation): string {
  if (tree.kind === "clean") return `<p class="compact-fact">Working Tree: <strong>clean</strong></p>`;
  return facts([{ label: "状態", value: "changesあり" }, { label: "Unstaged", value: String(tree.unstagedCount) }, { label: "Modified", value: String(tree.modifiedCount) }, { label: "Untracked", value: String(tree.untrackedCount) }, { label: "Conflicts", value: String(tree.conflictsCount) }]);
}

function stash(value: import("./gitMapPresentation").StashPresentation): string {
  if (value.kind === "none") return "";
  if (value.kind === "unavailable") return `<aside class="stash-unknown unknown-state" title="${escapeHtml(value.reason)}"><span class="unknown-symbol">?</span>Stash情報を取得できません</aside>`;
  return `<aside class="stash-shelf fact-state${visualClass(value.visualState)}"${selectionAttr({ kind: "stashShelf" }, value.visualState === "selected")}>Stash Shelf · ${value.count}件</aside>`;
}

function unknown(value: import("./gitMapPresentation").GitMapUnknown, reason?: string): string { return `<p class="unknown-state"${reason ? ` title="${escapeHtml(reason)}"` : ""}><span class="unknown-symbol">?</span>${escapeHtml(value.label)}${value.message ? `：${escapeHtml(value.message)}` : ""}</p>`; }

function facts(items: readonly GitMapItem[]): string { return `<dl>${items.map((item) => `<div><dt>${escapeHtml(item.label)}</dt><dd>${escapeHtml(item.value)}</dd></div>`).join("")}</dl>`; }
function visualClass(value: "selected" | "related" | undefined): string { return value ? ` ${value}-state` : ""; }
function selectionAttr(selection: unknown, primary = false): string { return ` data-selection="${escapeHtml(JSON.stringify(selection))}" tabindex="0" role="button"${primary ? ' data-primary-selection="true"' : ""}`; }
